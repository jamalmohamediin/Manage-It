// src/hooks/useBusinessId.ts
export function useBusinessId(): string {
  // For now, hardcoded — later can be context-driven
    // Later this can come from auth or context

  return 'demo-business-id-001';
}

