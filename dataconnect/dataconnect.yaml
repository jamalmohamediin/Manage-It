specVersion: "v1"
serviceId: "manage-it"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "manage-it-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
